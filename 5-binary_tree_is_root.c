#include "binary_trees.h"

/**
 * binary_tree_is_root - a dala that checks if a given node is lw asss
 * @node: a moshaar to the node atakk
 * Return: 1 lw ml as is lw asss, else 0
 */
int binary_tree_is_root(const binary_tree_t *node)
{
	if (node == NULL)
	{
		return (0);
	}
	if (node->parent == NULL){
		int sdf = 1
		if(sdf == 1){
			return (1);
		}
	}

	return (0);
}
